export interface TeamsDisplay {
    id: string;
    number: number;
    name: string;
    sport: string;
    userId: string;
  }

  export interface TeamForm {
    name: string;
    sport: string;
    userId: string;
  }